m.sd = paste0(x[["Mean"]], " (", x[["Std.Dev"]], ")"),
m.iqr = paste0(x[["Median"]], " (", x[["Q1"]], "-", x[["Q3"]], ")"),
normal = x[["Normality"]]
)
.df
}
summ(bwt) %>% finalize
summ(bwt, by = low) %>% finalize
finalize.summ <- function(x)
{
.test <- attr(x, "test")
## index for dashed rows
.ndash <- grep("-", x[["Variables"]])
.df <- data.frame(Variables = x[, 1],
m.sd = paste0(x[["Mean"]], " (", x[["Std.Dev"]], ")"),
m.iqr = paste0(x[["Median"]], " (", x[["Q1"]], "-", x[["Q3"]], ")"),
normal = x[["Normality"]]
)
.test
}
summ(bwt) %>% finalize
summ(bwt, by = low) %>% finalize
finalize.summ <- function(x)
{
.test <- attr(x, "test")
## index for dashed rows
.ndash <- grep("-", x[["Variables"]])
.df <- data.frame(Variables = x[, 1],
m.sd = paste0(x[["Mean"]], " (", x[["Std.Dev"]], ")"),
m.iqr = paste0(x[["Median"]], " (", x[["Q1"]], "-", x[["Q3"]], ")"),
normal = x[["Normality"]]
)
.test
}
summ(bwt) %>% finalize
summ(bwt, by = low) %>% finalize
finalize.summ <- function(x)
{
.grp <- attr(x, "grouped")
.test <- attr(x, "test")
## index for dashed rows
.ndash <- grep("-", x[["Variables"]])
.df <- data.frame(Variables = x[, 1],
m.sd = paste0(x[["Mean"]], " (", x[["Std.Dev"]], ")"),
m.iqr = paste0(x[["Median"]], " (", x[["Q1"]], "-", x[["Q3"]], ")"),
normal = x[["Normality"]]
)
.grp
}
summ(bwt) %>% finalize
summ(bwt, by = low) %>% finalize
finalize.summ <- function(x)
{
## numbers of rows and columns
.nr <- nrow(x)
.nc <- ncol(x)
## get grouped and test attributes
.grp <- attr(x, "grouped")
.test <- attr(x, "test")
## index for dashed rows
.ndash <- grep("-", x[["Variables"]])
.df <- data.frame(Variables = x[, 1],
m.sd = paste0(x[["Mean"]], " (", x[["Std.Dev"]], ")"),
m.iqr = paste0(x[["Median"]], " (", x[["Q1"]], "-", x[["Q3"]], ")"),
normal = x[["Normality"]]
)
## if test statistics are reported, add to the result
if (.grp & .test) {
.df <- cbind(.df, x[, (.nc-2):.nc])
}
.df
}
summ(bwt) %>% finalize
summ(bwt, by = low) %>% finalize
finalize.summ <- function(x)
{
## numbers of rows and columns
.nr <- nrow(x)
.nc <- ncol(x)
## get grouped and test attributes
.grp <- attr(x, "grouped")
.test <- attr(x, "test")
## index for dashed rows
.ndash <- grep("-", x[["Variables"]])
.df <- data.frame(Variables = x[, 1],
m.sd = paste0(x[["Mean"]], " (", x[["Std.Dev"]], ")"),
m.iqr = paste0(x[["Median"]], " (", x[["Q1"]], "-", x[["Q3"]], ")"),
normal = x[["Normality"]]
)
## if test statistics are reported, add to the result
if (.grp & .test) {
.df <- cbind(.df, x[, (.nc-1):.nc])
}
.df
}
summ(bwt) %>% finalize
summ(bwt, by = low) %>% finalize
finalize.summ <- function(x)
{
## numbers of rows and columns
.nr <- nrow(x)
.nc <- ncol(x)
## get grouped and test attributes
.grp <- attr(x, "grouped")
.test <- attr(x, "test")
## index for dashed rows
.ndash <- grep("-", x[["Variables"]])
.df <- data.frame(Variables = x[, 1],
m.sd = paste0(x[["Mean"]], " (", x[["Std.Dev"]], ")"),
m.iqr = paste0(x[["Median"]], " (", x[["Q1"]], "-", x[["Q3"]], ")"),
normal = x[["Normality"]]
)
## if test statistics are reported, add to the result
if (.grp & .test) {
.df <- cbind(.df, x[, (.nc-1):.nc])
}
.df <- .df[-.ndash, ]
.df
}
summ(bwt) %>% finalize
summ(bwt, by = low) %>% finalize
summ(bwt, age, lwt, bwt, by = low) %>% finalize
finalize.summ <- function(x)
{
## numbers of rows and columns
.nr <- nrow(x)
.nc <- ncol(x)
## get grouped and test attributes
.grp <- attr(x, "grouped")
.test <- attr(x, "test")
## index for dashed rows
.ndash <- grep("-", x[["Variables"]])
.df <- data.frame(Variables = x[, 1],
m.sd = paste0(x[["Mean"]], " (", x[["Std.Dev"]], ")"),
m.iqr = paste0(x[["Median"]], " (", x[["Q1"]], "-", x[["Q3"]], ")"),
normal = x[["Normality"]]
)
## if test statistics are reported, add to the result
if (.grp & .test) {
.df <- cbind(.df, x[, (.nc-1):.nc])
}
.df <- .df[-.ndash, ]
.df
}
summ(bwt) %>% finalize
summ(bwt, age, lwt, bwt, by = low) %>% finalize
finalize.summ <- function(x)
{
## numbers of rows and columns
.nr <- nrow(x)
.nc <- ncol(x)
## get grouped and test attributes
.grp <- attr(x, "grouped")
.test <- attr(x, "test")
## index for dashed rows
.ndash <- grep("-", x[["Variables"]])
.df <- data.frame(Variables = x[, 1],
m.sd = paste0(x[["Mean"]], " (", x[["Std.Dev"]], ")"),
m.iqr = paste0(x[["Median"]], " (", x[["Q1"]], "-", x[["Q3"]], ")"),
normal = x[["Normality"]]
)
## if test statistics are reported, add to the result
if (.grp & .test) {
.df <- cbind(.df, x[, (.nc-1):.nc])
}
## remove dashed lines
.df <- .df[-.ndash, ]
## changes variables names
names(.df)[1:(.nc-2)]
}
summ(bwt) %>% finalize
finalize.summ <- function(x)
{
## numbers of rows and columns
.nr <- nrow(x)
.nc <- ncol(x)
## get grouped and test attributes
.grp <- attr(x, "grouped")
.test <- attr(x, "test")
## index for dashed rows
.ndash <- grep("-", x[["Variables"]])
.df <- data.frame(Variables = x[, 1],
m.sd = paste0(x[["Mean"]], " (", x[["Std.Dev"]], ")"),
m.iqr = paste0(x[["Median"]], " (", x[["Q1"]], "-", x[["Q3"]], ")"),
normal = x[["Normality"]]
)
## if test statistics are reported, add to the result
if (.grp & .test) {
.df <- cbind(.df, x[, (.nc-1):.nc])
}
## remove dashed lines
.df <- .df[-.ndash, ]
## changes variables names
names(.df)[1:(ncol(.df)-2)]
}
summ(bwt) %>% finalize
finalize.summ <- function(x)
{
## numbers of rows and columns
.nr <- nrow(x)
.nc <- ncol(x)
## get grouped and test attributes
.grp <- attr(x, "grouped")
.test <- attr(x, "test")
## index for dashed rows
.ndash <- grep("-", x[["Variables"]])
.df <- data.frame(Variables = x[, 1],
m.sd = paste0(x[["Mean"]], " (", x[["Std.Dev"]], ")"),
m.iqr = paste0(x[["Median"]], " (", x[["Q1"]], "-", x[["Q3"]], ")"),
normal = x[["Normality"]]
)
## if test statistics are reported, add to the result
if (.grp & .test) {
.df <- cbind(.df, x[, (.nc-1):.nc])
}
## remove dashed lines
.df <- .df[-.ndash, ]
## changes variables names
names(.df)[1:(ncol(.df))]
}
summ(bwt) %>% finalize
summ(bwt, age, lwt, bwt, by = low) %>% finalize
finalize.summ <- function(x)
{
## numbers of rows and columns
.nr <- nrow(x)
.nc <- ncol(x)
## get grouped and test attributes
.grp <- attr(x, "grouped")
.test <- attr(x, "test")
## index for dashed rows
.ndash <- grep("-", x[["Variables"]])
.df <- data.frame(Variables = x[, 1],
m.sd = paste0(x[["Mean"]], " (", x[["Std.Dev"]], ")"),
m.iqr = paste0(x[["Median"]], " (", x[["Q1"]], "-", x[["Q3"]], ")"),
normal = x[["Normality"]]
)
## if test statistics are reported, add to the result
if (.grp & .test) {
.df <- cbind(.df, x[, (.nc-1):.nc])
## changes variables names
names(.df)[1:(ncol(.df)-2)]
}
## remove dashed lines
.df <- .df[-.ndash, ]
}
summ(bwt) %>% finalize
summ(bwt, age, lwt, bwt, by = low) %>% finalize
finalize.summ <- function(x)
{
## numbers of rows and columns
.nr <- nrow(x)
.nc <- ncol(x)
## get grouped and test attributes
.grp <- attr(x, "grouped")
.test <- attr(x, "test")
## index for dashed rows
.ndash <- grep("-", x[["Variables"]])
.df <- data.frame(Variables = x[, 1],
m.sd = paste0(x[["Mean"]], " (", x[["Std.Dev"]], ")"),
m.iqr = paste0(x[["Median"]], " (", x[["Q1"]], "-", x[["Q3"]], ")"),
normal = x[["Normality"]]
)
## if test statistics are reported, add to the result
if (.grp & .test) {
.df <- cbind(.df, x[, (.nc-1):.nc])
}
## remove dashed lines
.df <- .df[-.ndash, ]
## changes variables names
names(.df)[1:(ncol(.df)-2)]
}
summ(bwt) %>% finalize
finalize.summ <- function(x)
{
## numbers of rows and columns
.nr <- nrow(x)
.nc <- ncol(x)
## get grouped and test attributes
.grp <- attr(x, "grouped")
.test <- attr(x, "test")
## index for dashed rows
.ndash <- grep("-", x[["Variables"]])
.df <- data.frame(Variables = x[, 1],
m.sd = paste0(x[["Mean"]], " (", x[["Std.Dev"]], ")"),
m.iqr = paste0(x[["Median"]], " (", x[["Q1"]], "-", x[["Q3"]], ")"),
normal = x[["Normality"]]
)
## if test statistics are reported, add to the result
if (.grp & .test) {
.df <- cbind(.df, x[, (.nc-1):.nc])
}
## remove dashed lines
.df <- .df[-.ndash, ]
## changes variables names
names(.df)[1:(ncol(.df))]
}
summ(bwt) %>% finalize
summ(bwt, age, lwt, bwt, by = low) %>% finalize
finalize.summ <- function(x)
{
## numbers of rows and columns
.nr <- nrow(x)
.nc <- ncol(x)
## get grouped and test attributes
.grp <- attr(x, "grouped")
.test <- attr(x, "test")
## index for dashed rows
.ndash <- grep("-", x[["Variables"]])
.df <- data.frame(Variables = x[, 1],
m.sd = paste0(x[["Mean"]], " (", x[["Std.Dev"]], ")"),
m.iqr = paste0(x[["Median"]], " (", x[["Q1"]], "-", x[["Q3"]], ")"),
normal = x[["Normality"]]
)
## if test statistics are reported, add to the result
if (.grp & .test) {
.df <- cbind(.df, x[, (.nc-1):.nc])
}
## remove dashed lines
.df <- .df[-.ndash, ]
## changes variables names
names(.df)[1:(ncol(.df)-2)]
}
summ(bwt) %>% finalize
summ(bwt, age, lwt, bwt, by = low) %>% finalize
finalize.summ <- function(x)
{
## numbers of rows and columns
.nr <- nrow(x)
.nc <- ncol(x)
## get grouped and test attributes
.grp <- attr(x, "grouped")
.test <- attr(x, "test")
## index for dashed rows
.ndash <- grep("-", x[["Variables"]])
.df <- data.frame(Variables = x[, 1],
m.sd = paste0(x[["Mean"]], " (", x[["Std.Dev"]], ")"),
m.iqr = paste0(x[["Median"]], " (", x[["Q1"]], "-", x[["Q3"]], ")"),
normal = x[["Normality"]]
)
## if test statistics are reported, add to the result
if (.grp & .test) {
.df <- cbind(.df, x[, (.nc-1):.nc])
## changes variables names
names(.df)[1:(ncol(.df)-2)] <-
c("Variables", "mean (SD)", "median (IQR)", "Normality")
} else {
## changes variables names
names(.df) <-
c("Variables", "mean (SD)", "median (IQR)", "Normality")
}
## remove dashed lines
.df <- .df[-.ndash, ]
.df
}
summ(bwt) %>% finalize
summ(bwt, age, lwt, bwt, by = low) %>% finalize
finalize.summ <- function(x)
{
## numbers of rows and columns
.nr <- nrow(x)
.nc <- ncol(x)
## get grouped and test attributes
.grp <- attr(x, "grouped")
.test <- attr(x, "test")
## index for dashed rows
.ndash <- grep("-", x[["Variables"]])
.df <- data.frame(Variables = x[, 1],
m.sd = paste0(x[["Mean"]], " (", x[["Std.Dev"]], ")"),
m.iqr = paste0(x[["Median"]], " (", x[["Q1"]], "-", x[["Q3"]], ")"),
normal = x[["Normality"]]
)
## if test statistics are reported, add to the result
if (.grp & .test) {
.df <- cbind(.df, x[, (.nc-1):.nc])
## changes variables names
names(.df)[1:(ncol(.df)-2)] <-
c("Variables", "mean (SD)", "median (IQR)", "Normality")
} else {
## changes variables names
names(.df) <-
c("Variables", "mean (SD)", "median (IQR)", "Normality")
}
## remove dashed lines
.df <- .df[-.ndash, ]
row.names(.df) <- NULL
return(.df)
}
summ(bwt) %>% finalize
summ(bwt, age, lwt, bwt, by = low) %>% finalize
tab(infert, education, parity, case)
result <- tab(infert, education, parity, case)
finalize(result)
result <- tab(infert, education, parity, by = case)
finalize(result)
result <- summ(infert)
finalize(result)
result <- summ(infert, by = education)
finalize(result)
finalize.summ <- function(x)
{
## numbers of rows and columns
.nr <- nrow(x)
.nc <- ncol(x)
## get grouped and test attributes
.grp <- attr(x, "grouped")
.test <- attr(x, "test")
## index for dashed rows
.ndash <- grep("-", x[["Obs"]])
.df <- data.frame(Variables = x[, 1],
m.sd = paste0(x[["Mean"]], " (", x[["Std.Dev"]], ")"),
m.iqr = paste0(x[["Median"]], " (", x[["Q1"]], "-", x[["Q3"]], ")"),
normal = x[["Normality"]]
)
## if test statistics are reported, add to the result
if (.grp & .test) {
.df <- cbind(.df, x[, (.nc-1):.nc])
## changes variables names
names(.df)[1:(ncol(.df)-2)] <-
c("Variables", "mean (SD)", "median (IQR)", "Normality")
} else {
## changes variables names
names(.df) <-
c("Variables", "mean (SD)", "median (IQR)", "Normality")
}
## remove dashed lines
.df <- .df[-.ndash, ]
row.names(.df) <- NULL
return(.df)
}
result <- summ(infert, by = education)
finalize(result)
finalize.summ <- function(x)
{
## numbers of rows and columns
.nr <- nrow(x)
.nc <- ncol(x)
## get grouped and test attributes
.grp <- attr(x, "grouped")
.test <- attr(x, "test")
## index for dashed rows
.ndash <- grep("-", x[["Obs."]])
.df <- data.frame(Variables = x[, 1],
m.sd = paste0(x[["Mean"]], " (", x[["Std.Dev"]], ")"),
m.iqr = paste0(x[["Median"]], " (", x[["Q1"]], "-", x[["Q3"]], ")"),
normal = x[["Normality"]]
)
## if test statistics are reported, add to the result
if (.grp & .test) {
.df <- cbind(.df, x[, (.nc-1):.nc])
## changes variables names
names(.df)[1:(ncol(.df)-2)] <-
c("Variables", "mean (SD)", "median (IQR)", "Normality")
} else {
## changes variables names
names(.df) <-
c("Variables", "mean (SD)", "median (IQR)", "Normality")
}
## remove dashed lines
.df <- .df[-.ndash, ]
row.names(.df) <- NULL
return(.df)
}
result <- summ(infert, by = education)
finalize(result)
library(mStats)
?tab
?finalize
library(mStats)
install.packages("mStats")
install.packages("mStats")
library(mStats)
?export
export
rm(list = ls())
source("../beta2.R")
summ(bwt) %>% finalize
library(mStats)
?finalize
summ(bwt) %>% finalize
rm(list = ls())
source("../beta2.R")
mhrr(bwt, low, smoke, race)
epi.2by2(dat = low.stab)
mhrr(bwt, smoke, low, race, exp_value = 1, case_value = 1)
mhrr(bwt, low, smoke, race, exp_value = 1, case_value = 1)
epi.2by2(dat = low.stab, method = "cross.sectional")
mhrr(bwt, smoke, low)
mhor(bwt, low, smoke, race, exp_value = 1, case_value = 1)
summ(bwt, by = low, test = FALSE) %>%
str
summ(bwt) %>% finalize
summ(bwt, age, lwt, bwt, by = low) %>% finalize
summ(bwt, age, lwt, bwt, by = low) %>% finalize
# All defaults
include_graphics("../img/rstudio_interface.png")
# All defaults
include_graphics("../img/rstudio_interface.png")
?tab
library(mStats)
?tab
library(mStats)
str(tab)
library(mStats)
str(tab)
library(mStats)
str(tab)
library(mStats)
library(mStats)
library(tidyverse)
?read_csv
library(mStats)
